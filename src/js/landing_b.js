(function($) {
    "use strict"
    $(function() {

        $('.bl-sx__items').slick({
            prevArrow: '.bl-sx__controls button.is--prev',
            nextArrow: '.bl-sx__controls button.is--next',
            adaptiveHeight: true
        });

    })
})(jQuery);